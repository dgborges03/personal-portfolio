<!-- ---
import Layout from "../../layouts/Layout.astro";
import { passions } from "../../data/passions.js";

export async function getStaticPaths() {
  return passions
    .filter((p) => p.link)
    .map((p) => ({
      params: { id: p.id },
      props: { passion: p },
    }));
}

const { passion } = Astro.props;
const heroImage = passion.detailImage || passion.image;
---
<Layout>
  ********** Save for when one of my projects have a "View Project" button **********
</Layout> -->

